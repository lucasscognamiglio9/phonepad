name: Host platforms

on:
  push:
    branches: [refactor/p00-baseline]
    paths:
      - '.github/workflows/host-platforms.yml'
      - 'daemon/**'
      - 'setup/preview/**'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  daemon:
    strategy:
      fail-fast: false
      matrix:
        runner: [macos-15, windows-2025]
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: daemon/go.mod
          cache-dependency-path: daemon/go.sum
      - name: Test shared server
        working-directory: daemon
        run: go test -mod=vendor ./internal/...
      - name: Build daemon
        if: always()
        working-directory: daemon
        run: go build -mod=vendor -o phonepad-daemon${{ runner.os == 'Windows' && '.exe' || '' }} .
      - name: Check Mac hostname parser
        if: runner.os == 'macOS'
        shell: bash
        run: |
          value=$(printf '{"Self":{"DNSName":"mac.tailnet.ts.net."}}' | plutil -extract Self.DNSName raw -o - -)
          test "$value" = 'mac.tailnet.ts.net.'
      - name: Stage host bundle
        if: success()
        shell: bash
        run: |
          mkdir -p dist
          cp daemon/phonepad-daemon${{ runner.os == 'Windows' && '.exe' || '' }} dist/
          if [ "${{ runner.os }}" = 'macOS' ]; then
            cp setup/hosts/start-mac.command dist/
            chmod +x dist/start-mac.command dist/phonepad-daemon
          else
            cp setup/hosts/start-windows.ps1 dist/
          fi
      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: phonepad-host-${{ matrix.runner }}
          path: dist/
